# Generated by Chef for <%= node['fqdn'] %>

iquerySecName   _internal

com2sec         local       192.168.2.0/24  private
com2sec         local       127.0.0.0/24    private

group           local_group v1              local
group           local_group v2c             local   
group           local_group usm             local

view            view_all    included        .1 80

access          local_group ""              any noauth exact view_all none none

extend          amb_temp /etc/snmp/hard_temp.zsh AMBIENT_THERMAL
extend          cpu_temp /etc/snmp/hard_temp.zsh CPU_THEMAL

extend          sda_temp /etc/snmp/disk_temp.zsh /dev/sda
extend          sdb_temp /etc/snmp/disk_temp.zsh /dev/sdb
extend          sdc_temp /etc/snmp/disk_temp.zsh /dev/sdc
extend          sdd_temp /etc/snmp/disk_temp.zsh /dev/sdd
extend          sde_temp /etc/snmp/disk_temp.zsh /dev/sde

disk            /storage 5%
disk            /backup 5%

syslocation     Home Gateway
syscontact      root@columbia.green-rabbit.net
