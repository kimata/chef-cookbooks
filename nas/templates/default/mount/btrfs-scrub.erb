#!/usr/bin/zsh
# Generated by Chef for <%= node['fqdn'] %>

VOLUMES=`df --local --type=btrfs | awk 'NR>1{ print $6, $1 }' | uniq -f 1 | awk '{print $1}'`;

for i in $VOLUMES
do
    btrfs scrub start $i;
done
